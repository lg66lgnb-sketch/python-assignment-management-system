# 给朋友的交付、运行与答辩说明

这份文档是给接手项目的人看的，不是提交给老师的正式文档。目标是说明：应该拿到哪些文件、Windows 本地怎么运行、怎么验证系统、答辩时怎么演示、最后应该提交哪些内容。

## 1. 应该发哪些文件

推荐直接发送整个项目文件夹，但发送前可以清理掉不需要的运行缓存。

必须保留：

- `assignment_system/`：系统主体代码，包括路由、服务类、数据库和页面模板。
- `scripts/`：数据库初始化、构建辅助脚本。
- `tests/`：自动化测试代码。
- `docs/pdf/`：三份正式文档 PDF。
- `docs/latex/`：三份正式文档的 LaTeX 源文件，方便需要时重新生成 PDF。
- `docs/screenshots/`：用户手册和过程剖析用到的截图。
- `app.py`、`main.py`、`requirements.txt`、`run_windows.bat`、`build_windows_exe.bat`、`README.md`。

可以不发或提交前删除：

- `.git/`、`.github/`：除非需要继续用 GitHub 或查看提交记录。
- `.venv/`：虚拟环境很大，接收方本地运行时会重新生成。
- `__pycache__/`、`.pytest_cache/`、临时 `tmp...` 文件夹：运行缓存。
- `data/assignment_system.sqlite3` 和 `data/uploads/`：如果想让接收方从干净演示数据开始，可以不发；运行 `scripts/init_db.py --reset` 会重新生成。

如果老师要求提交两个压缩包，建议这样分：

- 源程序压缩包：项目代码、脚本、测试、文档源文件、截图，不包含 `.venv/`。
- 执行程序及运行环境压缩包：如果已经成功打包，则放 `dist/` 或打包产物；如果现场主要用源码运行，也要说明源码运行是主方案。

## 2. Windows 本地如何运行

最简单方式：

1. 解压项目文件夹，路径建议不要太深，例如 `D:\python-coursework`。
2. 双击 `run_windows.bat`。
3. 脚本会自动创建 `.venv`、安装依赖、初始化数据库并启动系统。
4. 浏览器访问 `http://127.0.0.1:5000`。

默认演示账号：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 管理员 | `admin` | `admin123` |
| 教师 | `teacher01` | `teacher123` |
| 学生 | `student01` | `student123` |

如果想手动运行：

```bat
py -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python scripts\init_db.py --reset
python app.py
```

验证代码是否正常：

```bat
.venv\Scripts\python -m unittest discover -s tests -v
```

页面验证建议按这个顺序走一遍：

1. 学生登录，查看课程和作业。
2. 学生提交作业说明或附件。
3. 退出后教师登录，查看提交记录。
4. 教师评分并保存评语。
5. 教师导出 CSV 统计。
6. 管理员登录，查看用户统计和教师审核功能。

## 3. 答辩时怎么做

答辩不要一上来只点页面，建议按“设计、实现、演示、问题剖析”的顺序讲。

推荐流程：

1. 先说明系统主题：这是一个基于 Flask + SQLite 的作业管理数据库应用系统。
2. 打开《系统设计说明书》，讲三类用户：管理员、教师、学生。
3. 指出系统对象：用户、课程、选课记录、作业、提交记录、统计报表。
4. 打开系统页面，按学生、教师、管理员顺序演示完整业务流。
5. 演示导出 CSV、上传附件、评分这些能体现复杂度的功能。
6. 打开《过程剖析》，说明开发中遇到的 bug、如何复现、如何定位、如何修复。
7. 最后打开《用户使用手册》，说明普通用户可以照着手册完成操作。

答辩时重点强调：

- 系统不是单页面演示，而是有三类角色和完整权限控制。
- 数据存在 SQLite 中，不是写死在页面里。
- 密码使用哈希保存。
- 教师账号需要管理员审核。
- 学生提交、教师评分、导出统计形成完整闭环。
- 过程剖析里的 bug 都有 before/after 截图和代码，不是事后编造。

如果现场出问题：

- 页面打不开：确认 `run_windows.bat` 是否还在运行，访问地址是否是 `http://127.0.0.1:5000`。
- 数据不对：运行 `python scripts\init_db.py --reset` 重置演示数据。
- 依赖缺失：重新执行 `pip install -r requirements.txt`。
- 端口被占用：关闭其他 Flask/Python 窗口后重新运行。

## 4. 最后提交清单

根据作业要求，最终至少应提交：

- 《系统设计说明书》PDF。
- 《用户使用手册》PDF。
- 《过程剖析》PDF。
- 源程序压缩包。
- 执行程序及运行环境压缩包。

建议额外确认：

- 三份 PDF 中截图能正常显示。
- 过程剖析中的 bug 截图、before code、after code 都齐全。
- `run_windows.bat` 在 Windows 上可以启动系统。
- 默认账号可以正常登录。
- 源程序压缩包中没有 `.venv/` 这种大体积缓存目录。
- 如果提交 exe 包，先在一台 Windows 电脑上试运行一次。
