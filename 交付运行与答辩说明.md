# 给朋友的交付、运行与答辩说明

这份说明是给接手项目、帮忙测试或一起答辩的人看的，不是提交给老师的正式材料。

核心目标：让对方知道该拿哪些文件、如何在 Windows 本地跑起来、怎么验证系统功能、答辩时按什么顺序展示、最后应该提交哪些文件。

## 0. 最快结论

答辩现场优先准备这 4 个文件：

- `dist/执行程序及运行环境压缩包.zip`
- `docs/pdf/用户使用手册.pdf`
- `docs/pdf/系统设计说明书.pdf`
- `docs/pdf/过程剖析.pdf`

最终提交给老师时，再加上：

- `dist/源程序压缩包.zip`

不要把这份 `交付运行与答辩说明.md` 交给老师，它只是内部操作说明。

## 1. 应该发给朋友哪些文件

如果朋友只是帮忙测试运行和答辩流程，发下面这些就够了：

- `dist/执行程序及运行环境压缩包.zip`
- `docs/pdf/用户使用手册.pdf`
- `docs/pdf/系统设计说明书.pdf`
- `docs/pdf/过程剖析.pdf`
- `交付运行与答辩说明.md`

如果朋友还需要看源码、帮忙改代码或检查提交内容，再额外发：

- `dist/源程序压缩包.zip`

不建议直接发整个项目文件夹给老师。整个文件夹里可能有开发缓存、虚拟环境、Git 记录、临时构建文件，体积大，也不够干净。给老师看和提交时，优先使用 `dist/` 里已经整理好的两个压缩包。

## 2. Windows 本地如何运行

### 方式一：用执行程序包运行，答辩优先用这个

1. 解压 `dist/执行程序及运行环境压缩包.zip`。
2. 进入解压后的文件夹。
3. 双击 `启动系统.bat`。
4. 如果出现黑色命令行窗口，并显示 `Running on http://127.0.0.1:5000`，说明系统已经启动。
5. 浏览器访问 `http://127.0.0.1:5000`。

默认账号：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 管理员 | `admin` | `admin123` |
| 教师 | `teacher01` | `teacher123` |
| 学生 | `student01` | `student123` |

注意：命令行里的 `WARNING: This is a development server...` 是 Flask 本地运行的固定提示，不是错误。答辩演示可以正常使用。

测试结束后，在黑色命令行窗口按 `Ctrl + C` 关闭服务。

### 方式二：用源程序包运行，老师问源码或环境时再用

1. 解压 `dist/源程序压缩包.zip`。
2. 进入解压后的项目文件夹。
3. 双击 `run_windows.bat`。
4. 脚本会自动创建 `.venv`、安装依赖、初始化数据库并启动系统。
5. 浏览器访问 `http://127.0.0.1:5000`。

如果想手动运行，可以在项目根目录执行：

```bat
py -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python scripts\init_db.py --reset
python app.py
```

验证测试：

```bat
.venv\Scripts\python -m unittest discover -s tests -v
```

## 3. 自己亲手测试时怎么走

建议用一个全新的测试文件夹，不要直接在开发目录里测。

1. 在桌面新建 `coursework_test` 文件夹。
2. 把 `执行程序及运行环境压缩包.zip` 复制进去并解压。
3. 双击 `启动系统.bat`。
4. 打开 `http://127.0.0.1:5000`。
5. 用学生账号 `student01 / student123` 登录，查看课程和作业。
6. 提交一次作业，可以上传一个中文文件名附件，例如 `数据库练习报告.docx`。
7. 退出学生账号。
8. 用教师账号 `teacher01 / teacher123` 登录，查看学生提交记录。
9. 给提交记录评分、填写评语并保存。
10. 回到学生账号，确认能看到成绩和评语。
11. 用管理员账号 `admin / admin123` 登录，查看用户和教师审核相关页面。

如果这条流程能跑完，答辩时基本就稳了。

## 4. 答辩时怎么讲

推荐顺序：

1. 先打开 `系统设计说明书.pdf`，说明项目是基于 Flask + SQLite 的作业管理系统。
2. 讲系统角色：管理员、教师、学生。
3. 讲核心数据对象：用户、课程、选课记录、作业、提交记录、评分记录。
4. 启动系统，进入登录页。
5. 先用学生账号演示查看作业和提交作业。
6. 再用教师账号演示查看提交、评分、写评语。
7. 最后用管理员账号演示用户管理和教师审核。
8. 打开 `过程剖析.pdf`，讲开发中遇到的 bug、复现方式、before/after 代码和截图。
9. 打开 `用户使用手册.pdf`，说明普通用户可以按手册完成操作。

答辩时重点强调：

- 系统不是单页面展示，而是有三类角色和权限区分。
- 数据保存在 SQLite 数据库中，不是写死在页面里。
- 密码使用哈希保存。
- 教师账号需要管理员审核。
- 学生提交、教师评分、学生查看结果形成完整闭环。
- 过程剖析中的 bug 都有真实复现截图、修复前后代码和修复后效果截图。
- 中文文件名上传问题已经修复，系统会保留用户看到的原始文件名。

## 5. 最终提交清单

建议最终提交这 5 个：

- `dist/源程序压缩包.zip`
- `dist/执行程序及运行环境压缩包.zip`
- `docs/pdf/用户使用手册.pdf`
- `docs/pdf/系统设计说明书.pdf`
- `docs/pdf/过程剖析.pdf`

提交前再检查一遍：

- 两个压缩包都能正常解压。
- `执行程序及运行环境压缩包.zip` 解压后能通过 `启动系统.bat` 启动。
- 三个 PDF 都能正常打开，截图显示完整。
- `过程剖析.pdf` 里每个 bug 都有 before/after 的代码和截图。
- 不提交 `.venv/`、`.git/`、`build/`、开发过程说明、内部备忘文档。

## 6. 现场常见问题

页面打不开：

- 确认黑色命令行窗口还开着。
- 确认访问地址是 `http://127.0.0.1:5000`。
- 如果端口被占用，先关闭其他 Python 或 Flask 窗口，再重新双击 `启动系统.bat`。

账号登录不了：

- 确认使用的是默认账号。
- 如果数据被改乱，删除运行目录里的 `data` 文件夹，然后重新解压运行环境包。

老师问源码在哪里：

- 打开 `源程序压缩包.zip`，重点看 `assignment_system/`、`templates/`、`static/`、`schema.sql`、`tests/`。

老师问为什么有黑窗口：

- 这是本地 Web 系统的运行服务窗口。浏览器页面依赖它提供后台服务，测试结束后按 `Ctrl + C` 关闭即可。
